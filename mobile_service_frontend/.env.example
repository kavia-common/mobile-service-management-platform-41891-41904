# Frontend configuration
# Base URL for the backend API (prefer setting this in preview/deployment env)
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional: if true, skip wait-for-backend polling (pairs with backend toggle)
BACKEND_HEALTHCHECK_DISABLED=false
